# Topics

Each folder is a standalone topic.  
Run commands from inside the folder you are exploring.

## Infrastructure

- terraform-aws-ec2-node  
  Single EC2 instance with cloud-init, Node, nginx, and real startup behavior
